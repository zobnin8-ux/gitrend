name: Weekly Radar Report

on:
  schedule:
    # Суббота 18:00 UTC = 21:00 МСК — файл готов к воскресенью утром для Радара
    - cron: "0 18 * * 6"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  weekly-radar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build native modules
        run: npm rebuild better-sqlite3

      - name: Restore SQLite database
        uses: actions/cache@v4
        with:
          path: data/github-trends.db
          key: radar-db-${{ github.repository }}
          restore-keys: |
            radar-db-

      - name: Ensure data directory
        run: mkdir -p data

      - name: Refresh GitHub data
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run radar:refresh

      - name: Generate weekly radar JSON
        run: npm run radar:weekly

      - name: Commit report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/weekly-radar.json
          if git diff --staged --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "chore(radar): weekly report $(node -p "require('./reports/weekly-radar.json').week")"
          git push
