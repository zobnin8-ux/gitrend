# GitHub Тренды — трекер популярных и быстрорастущих репозиториев

Локальное русскоязычное веб-приложение для отслеживания самых популярных и
быстрее всего набирающих звёзды GitHub-репозиториев. Хранит историю изменений,
строит графики роста и помогает видеть технологические тренды.

> Приложение предназначено **только для локального использования**: без
> авторизации, без пользователей, без внешних баз данных. Все данные хранятся
> локально в SQLite.

## Возможности

- **Обзор** — сводные карточки: всего репозиториев, новых за неделю/месяц,
  самый быстрорастущий, самый популярный, самый популярный язык.
- **Популярные сейчас** — список по общему количеству звёзд.
- **Быстрорастущие за 24 часа / 7 дней / 30 дней** — рост звёзд за период,
  абсолютный и в процентах.
- **Новые проекты** — фильтр по периоду создания (неделя / месяц / квартал).
- **Избранное** — личный список отмеченных репозиториев.
- **Поиск** — глобальный поиск по названию, описанию и темам.
- **Страница репозитория** — общая информация, текущая статистика, показатели
  роста и линейные графики Stars/Forks за 7 / 30 / 90 дней и всё время.
- **Фильтры** — по названию, описанию, языку, теме, количеству звёзд, возрасту
  и активности.
- **История** — каждое обновление создаёт snapshot (Stars, Forks, Issues, дата).
- **AI-описания (опционально)** — при наличии `OPENAI_API_KEY` для репозиториев
  генерируется краткое описание на русском (2–3 предложения).

## Технологический стек

- **Next.js 14** (App Router) + **TypeScript**
- **Tailwind CSS**
- **SQLite** через `better-sqlite3`
- **GitHub REST API** (через встроенный `fetch`)
- **OpenAI API** — опционально
- Графики — собственный лёгкий SVG-компонент (без внешних библиотек)

## Требования

- **Node.js 18 или 20** (рекомендуется LTS)
- Аккаунт GitHub и **Personal Access Token** (read-only достаточно)

## Установка и запуск

1. Установить зависимости:

```bash
npm install
```

2. Создать файл `.env.local` на основе примера и заполнить токен:

```bash
copy .env.example .env.local   # Windows (PowerShell/CMD)
# cp .env.example .env.local    # macOS / Linux
```

Содержимое `.env.local`:

```env
GITHUB_TOKEN=ghp_ваш_токен
OPENAI_API_KEY=          # необязательно
```

Токен создаётся здесь: https://github.com/settings/tokens
(достаточно прав на чтение публичных репозиториев).

3. Запустить в режиме разработки:

```bash
npm run dev
```

Открыть http://localhost:3000

4. Нажать кнопку **«Обновить данные»** в правом верхнем углу — приложение
   загрузит репозитории из GitHub, создаст первые snapshots и заполнит базу.

> **Важно про показатели роста.** Рост за 24 часа / 7 / 30 дней считается по
> разнице снапшотов. После первого обновления история состоит из одной точки,
> поэтому рост будет нулевым. Чтобы увидеть динамику и графики, обновляйте
> данные периодически (например, раз в день) — история будет накапливаться.

## Сборка production-версии

```bash
npm run build
npm run start
```

## Структура проекта

```txt
/app
  /api
    /refresh         POST — обновление данных из GitHub
    /repositories    GET  — список репозиториев с ростом и фильтрами
    /favorites       GET/POST — избранное
  /popular           Популярные сейчас
  /trending/24h|7d|30d  Быстрорастущие
  /new               Новые проекты
  /favorites         Избранные репозитории
  /search            Глобальный поиск
  /repo/[id]         Страница репозитория с графиками
  page.tsx           Главная (обзор)

/components
  RepositoryTable    Таблица репозиториев
  Filters            Панель фильтров
  StatsCards         Карточки статистики
  GrowthChart        SVG-график роста
  RepositoryExplorer Загрузка данных + фильтры + таблица
  Navbar / RefreshButton / FavoriteButton / SearchView / NewProjectsView

/lib
  github.ts          Клиент GitHub REST API и логика обновления
  sqlite.ts          Подключение к БД, схема и запросы
  analytics.ts       Расчёт показателей роста
  filters.ts         Фильтрация и сортировка
  stats.ts           Сводная статистика для обзора
  ai.ts              Опциональные AI-описания (OpenAI)
  format.ts          Форматирование для русского интерфейса
  types.ts           Общие типы

/data
  github-trends.db   Локальная база SQLite (создаётся автоматически)
```

## Источники данных (GitHub)

При обновлении опрашиваются направления: общий поиск популярных проектов,
Trending, AI, LLM, Agents, Automation, Developer Tools, Open Source и New
Repositories. Результаты объединяются и дедуплицируются по GitHub ID.

## Расчёт показателей

- **Рост за период** = текущие Stars − Stars на начало периода (по снапшотам).
- **Рост в %** = (новые Stars / старые Stars) × 100.
- **Средний рост в день** = рост за период / число дней.

## Замечания

- База данных (`/data/*.db`) добавлена в `.gitignore`.
- Лимиты GitHub Search API: с токеном ~30 запросов в минуту — обновление
  укладывается в лимит (9 запросов за раз).
```
